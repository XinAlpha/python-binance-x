========================================================================
                    策略汇总 - 6种交易策略
========================================================================

✅ 已添加5种新策略！现在共有6种策略可用：

========================================================================
                    策略列表
========================================================================

1. 均线交叉策略 (MA Crossover Strategy)
   文件: ma_crossover_strategy.py
   类名: MACrossoverStrategy
   适用: 趋势市场
   难度: ⭐ 简单
   特点: 经典策略，适合新手

2. RSI超买超卖策略 (RSI Strategy) ✨新增
   文件: rsi_strategy.py
   类名: RSIStrategy
   适用: 震荡市场
   难度: ⭐⭐ 中等
   特点: 高胜率，低风险

3. 布林带突破策略 (Bollinger Bands Strategy) ✨新增
   文件: bollinger_bands_strategy.py
   类名: BollingerBandsStrategy
   适用: 全市场
   难度: ⭐⭐ 中等
   特点: 自适应，两种模式

4. 网格交易策略 (Grid Trading Strategy) ✨新增
   文件: grid_trading_strategy.py
   类名: GridTradingStrategy
   适用: 震荡市场
   难度: ⭐⭐⭐ 复杂
   特点: 震荡市必杀技

5. MACD趋势策略 (MACD Strategy) ✨新增
   文件: macd_strategy.py
   类名: MACDStrategy
   适用: 趋势市场
   难度: ⭐⭐⭐ 复杂
   特点: 趋势跟踪，背离分析

6. 突破回踩策略 (Breakout Pullback Strategy) ✨新增
   文件: breakout_pullback_strategy.py
   类名: BreakoutPullbackStrategy
   适用: 趋势市场
   难度: ⭐⭐⭐⭐ 高级
   特点: 高风险高回报

========================================================================
                    如何使用新策略
========================================================================

方法1：修改配置文件
--------------------
编辑 config/config.yaml，修改策略名称和参数：

示例1 - 使用RSI策略：
```yaml
strategy:
  name: "rsi"
  params:
    rsi_period: 14
    oversold: 30
    overbought: 70
    stop_loss: 0.02
    take_profit: 0.03
```

示例2 - 使用布林带策略（均值回归模式）：
```yaml
strategy:
  name: "bollinger_bands"
  params:
    bb_period: 20
    bb_std: 2.0
    mode: "mean_reversion"  # 或 "trend"
    stop_loss: 0.025
    take_profit: 0.04
```

示例3 - 使用网格策略：
```yaml
strategy:
  name: "grid_trading"
  params:
    grid_num: 10
    price_range: 0.2
    grid_mode: "arithmetic"
    stop_loss: 0.05
```

示例4 - 使用MACD策略：
```yaml
strategy:
  name: "macd"
  params:
    fast_period: 12
    slow_period: 26
    signal_period: 9
    use_histogram: true
    use_zero_cross: false
    stop_loss: 0.025
    take_profit: 0.05
```

示例5 - 使用突破回踩策略：
```yaml
strategy:
  name: "breakout_pullback"
  params:
    lookback_period: 20
    breakout_threshold: 0.01
    pullback_ratio: 0.5
    use_volume: true
    stop_loss: 0.03
    take_profit: 0.06
```


方法2：修改main.py
--------------------
在 main.py 中导入并使用不同的策略：

```python
# 原来的代码
from strategies.ma_crossover_strategy import MACrossoverStrategy
strategy = MACrossoverStrategy(config['strategy'])

# 改为使用RSI策略
from strategies.rsi_strategy import RSIStrategy
strategy = RSIStrategy(config['strategy'])

# 或使用布林带策略
from strategies.bollinger_bands_strategy import BollingerBandsStrategy
strategy = BollingerBandsStrategy(config['strategy'])

# 或使用网格策略
from strategies.grid_trading_strategy import GridTradingStrategy
strategy = GridTradingStrategy(config['strategy'])

# 或使用MACD策略
from strategies.macd_strategy import MACDStrategy
strategy = MACDStrategy(config['strategy'])

# 或使用突破回踩策略
from strategies.breakout_pullback_strategy import BreakoutPullbackStrategy
strategy = BreakoutPullbackStrategy(config['strategy'])
```

========================================================================
                    策略参数详解
========================================================================

1. RSI策略参数
   - rsi_period: RSI计算周期，默认14
   - oversold: 超卖阈值，默认30（20-35范围）
   - overbought: 超买阈值，默认70（65-80范围）
   - use_divergence: 是否使用背离，默认false

2. 布林带策略参数
   - bb_period: 布林带周期，默认20
   - bb_std: 标准差倍数，默认2.0（1.5-2.5范围）
   - mode: 模式，"mean_reversion"或"trend"
   - squeeze_threshold: 挤压阈值，默认0.02

3. 网格策略参数
   - grid_num: 网格数量，默认10（5-20范围）
   - price_range: 价格区间，默认0.2（±20%）
   - grid_mode: "arithmetic"（等差）或"geometric"（等比）
   - rebalance_threshold: 重置阈值，默认0.3

4. MACD策略参数
   - fast_period: 快线周期，默认12
   - slow_period: 慢线周期，默认26
   - signal_period: 信号线周期，默认9
   - use_histogram: 是否用柱状图确认，默认true
   - use_zero_cross: 是否用零轴确认，默认false

5. 突破回踩策略参数
   - lookback_period: 回溯周期，默认20
   - breakout_threshold: 突破阈值，默认0.01
   - pullback_ratio: 回踩比例，默认0.5（50%）
   - use_volume: 是否用成交量确认，默认true
   - volume_multiplier: 成交量倍数，默认1.5

========================================================================
                    快速测试步骤
========================================================================

1. 测试策略导入
   python test_all_strategies.py

2. 选择一个策略并配置
   编辑 config/config.yaml

3. 运行回测
   python run_backtest.py

4. 分析结果，调整参数

5. 重复3-4直到满意

6. 模拟交易验证
   python main.py live

========================================================================
                    策略选择建议
========================================================================

新手推荐：
  1️⃣ 均线交叉策略 - 最简单，适合学习
  2️⃣ RSI策略 - 容易理解，胜率较高

进阶使用：
  3️⃣ 布林带策略 - 灵活自适应
  4️⃣ MACD策略 - 趋势跟踪能力强

高级策略：
  5️⃣ 网格策略 - 震荡市利器（需要充分理解）
  6️⃣ 突破回踩 - 高风险高回报（需要经验）

市场环境匹配：
  📈 趋势市场 → MACD、均线交叉、突破回踩
  📊 震荡市场 → RSI、网格交易、布林带（均值回归）
  ❓ 不确定   → 布林带（可切换模式）

========================================================================
                    注意事项
========================================================================

⚠️ 重要提醒：

1. 每个策略适用的市场环境不同
2. 必须先充分回测再实盘
3. 参数需要根据具体币种调整
4. 不存在"万能策略"
5. 严格执行止损止盈
6. 定期审查策略表现

建议工作流程：
  回测（1周）→ 参数优化（1周）→ 模拟交易（2周）→ 小额实盘（1月）→ 正式交易

========================================================================
                    文档和帮助
========================================================================

📄 详细策略文档：STRATEGIES.md
📖 使用指南：README.md
🚀 快速开始：QUICKSTART.md
💡 安装指南：INSTALL_GUIDE.md

========================================================================

现在有6种策略供您选择，可以根据不同市场环境灵活切换！

祝交易顺利！🚀💰

========================================================================
